# ── Archivos grandes / binarios pesados ───────────────────────────────────────
*.zip
vistas/img/cabeceras/cabeceras.zip

# ── Dependencias ──────────────────────────────────────────────────────────────
vendor/
node_modules/

# ── Variables de entorno (⚠️ contienen contraseñas de producción) ─────────────
.env
.env.local
.env.production
# .env.example SÍ se versiona — es la plantilla sin valores reales

# ── Logs y debug ──────────────────────────────────────────────────────────────
debug_log.txt
*.log
/logs/

# ── Caché y temporales ────────────────────────────────────────────────────────
*.cache
*.tmp

# ── PDFs generados por TCPDF ──────────────────────────────────────────────────
/extensiones/tcpdf/pdf/*.pdf

# ── IDE / editores ────────────────────────────────────────────────────────────
.idea/
.vscode/
*.swp
*~
.DS_Store
Thumbs.db
